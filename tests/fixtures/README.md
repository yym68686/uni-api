# Test Fixtures

This directory is the stable fixture root for offline unit tests.

- `adapters/`: provider request/response/stream golden fixtures.
- `sse/`: reusable SSE chunk and event fixtures.
- `config/`: reusable config compiler/runtime fixtures.

